name: Test
on:
  push:
    branches: [main]
    paths:
      - "*.ts"
      - denow
      - denow.bat
      - action.yml
      - .github/workflows/test.yml
  pull_request:
    branches: [main]
    paths:
      - "*.ts"
      - denow
      - denow.bat
      - action.yml
      - .github/workflows/test.yml
concurrency:
  group: test-${{ github.ref }}
  cancel-in-progress: true
permissions:
  contents: write
jobs:
  test:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v3
      - uses: ./
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          owner: ""
          secrets: '{ "MY_TEST": "test" }'
          dry-run: true
      - name: ./:filter
        uses: ./
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          owner: ""
          repository-filter: /sync-user-secrets$
          secrets: '{ "MY_TEST": "test" }'
          secret-filter: /TEST/
          dry-run: true
